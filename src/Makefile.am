## Process this file with automake to produce Makefile.in
# Makefile.am for The Machine Emulator:

AUTOMAKE_OPTIONS = 1.4 gnu
SUBDIRS = tme libltdl libtme generic ic machine host bus tmesh
EXTRA_DIST = modules
pkglib_DATA = tmeconfig.h
